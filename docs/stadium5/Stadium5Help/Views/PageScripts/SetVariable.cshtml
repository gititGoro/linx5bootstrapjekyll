<h1>Set Variable</h1>
<p>Set a variable in a script to use a value returned from one action in another or to build up a result of inputs and outputs combined</p>
<hr>
<h2>Properties</h2>
<ol>
    <li>
        <h4>Variables</h4>
        A list of variables linked to the script which can be assigned to or concatenated with other variables, script input parameters, script output parameters, script variables or text values
    </li>
</ol>